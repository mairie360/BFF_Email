name: BFF Email CICD #change bff name

on:
  push:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/BFFs-cicd.yml@mair-56-setup-open-api-gen
    with:
      package_name: bff-email #change bff name
      node_version: 20
    secrets: inherit